"use strict"; $(window).load(function () {
  $('#test').scrollToFixed(); var firebases = firebase.firestore(); var name = document.querySelector("#name"); var email = document.querySelector("#email"); var number = document.querySelector("#number"); var message = document.querySelector("#message"); var form = document.querySelector("#userqu"); var imageload = document.querySelector(".portfolioContainer"); form.addEventListener("submit", function (event) { event.preventDefault(); document.querySelector("#sendmessage").style.display = "none"; var dbRef = firebases.collection("users"); dbRef.add({ name: name.value, email: email.value, number: number.value, message: message.value, date: firebase.firestore.FieldValue.serverTimestamp() }).then(function (success) { return document.querySelector("#sendmessage").style.display = "block" }).catch(function (error) { return console.log(error.message) }) }); function initialize() { var $container = $('.portfolioContainer'), $body = $('body'), colW = 375, columns = null; $container.isotope({ resizable: !0, masonry: { columnWidth: colW } }); $(window).smartresize(function () { var currentColumns = Math.floor(($body.width() - 30) / colW); if (currentColumns !== columns) { columns = currentColumns; $container.width(columns * colW).isotope('reLayout') } }).smartresize(); $('.portfolioFilter a').click(function () { $('.portfolioFilter .current').removeClass('current'); $(this).addClass('current'); var selector = $(this).attr('data-filter'); $container.isotope({ filter: selector }); return !1 }) }
  initialize()
})